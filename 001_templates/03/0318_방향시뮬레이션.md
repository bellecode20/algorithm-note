- 기존 인덱스 안쓰고 갑자기 다시 내가 for문 돌면서 인덱스를 다시 생성해버림
- 다음 칸이 겹치는지 봐야되는데 현재 칸이 겹치는지 보고 있오
  -for문으로 방향 돌리고 있을 때 그냥 continue 해버려서 방향 안바뀌고 그냥 회전..

진짜 문제

물고기 정보를 range(1, len(fish)+1)로 잘못 순회함

방향을 원래 방향 d에서 시작하지 않음

while di < 8 + % 8 때문에 무한루프 가능

상어/냄새를 (r,c)가 아니라 (nr,nc)로 체크해야 함

continue 시 방향 회전 카운트가 안 됨

- 8방향 불가능하면 기존 방향 그대로 복사한 그래프에 넣어야 되는데 안넣음. 그냥 이동가능한것만 넣고, 처리없음. 8방향 불가능한거 Flag 처리했어야 함.

- 리스트로 만들었었는데 원소 넣을 때는 튜플로 넣고있음

- 냄새들이 나중에 사라져야된다고 생각해서 queue가 좋을 줄 알았는데, 아니었다.
  왜냐면 칸 끼리 겹치는 경우 문제 생길 수 있음. queue에 이미 이 냄새 기록된 칸 넣었는데
  큐에 또 추가되는 경우? 처음 칸이 2턴 지나서 그 칸 냄새 지웠는데 큐에는 또 다음 꺼 남아있으니까 이상해짐. 그러면 큐 하나로 끝나는게 아니라 보조 저장소가 또 필요하다. 현재 칸의 최신 냄새가 몇 턴 것인지..

  => set으로 하거나, 이차원배열로 하거나.
  set은 언제 괜찮냐
  set은 그 턴에 냄새 있는 칸만 빠르게 체크할 때는 좋아.

  예를 들면:
  smell = {(1,2), (3,0)}
  if (nr, nc) in smell:
  ...

  근데 냄새가 언제 생겼는지 정보가 없어서
  매 턴 제거하려면 결국 따로 턴 정보가 또 필요해.

  즉:

  단순 존재 여부 체크용 → set 괜찮음

  이 문제처럼 유지 기간이 있는 상태 → set만으로는 부족

- 상어는 상하좌우대각선이 아니라 상하좌우인데, 물고기랑 동일한 directions 리스트 썼음

- smell 남은 시간 기록하기. 매번 턴 끝나갈 때 모든 칸마다 -1 하면된다.
  그리고 나중에 냄새 칸 확인할 때 냄은시간 보면 됌

- extend 함수: 기존 리스트에 리스트 이어붙이기
- set()에서 삭제: visited에서 remove하기, add하기
