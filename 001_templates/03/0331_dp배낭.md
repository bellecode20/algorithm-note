```
def dp_1():  # 역순
    dp = [0] * (K + 1)

    for item in items:
        weight, value = item
        for i in range(K, weight - 1, -1):  # 현재 무게까지
            dp[i] = max(dp[i], dp[i - weight] + value)

    print(dp[-1])
```
정방향대로하면 이거 이 물건을 넣었는지 안넣었는지 어떻게 구분하지 했는데
역순으로 하면 해결됐다.